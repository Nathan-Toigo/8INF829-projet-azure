services:
  mongodb:
    image: mongo:7
    container_name: threetokens-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - threetokens_mongo_data:/data/db

volumes:
  threetokens_mongo_data:
